# e2e_smoke module0.5 真实链路验证 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `scripts/e2e_smoke.py` 末尾追加一段 module0.5 真实链路验证（①提议捕获 → ②入库 → ③继承降权 → ④真实judge回填 → ③精确升权），用真实 gpt-5.5 + 真实 Qwen embedding + 真实 Judge 跑通闭环。

**Architecture:** 纯脚本追加，零 src 改动。复用 main() 内已在作用域的 `emb`/`taxonomy`/`compiler`/`pipeline`/`gw` 变量 + 现有 0.5 API（ingest_proposal / run_backfill / rerank_with_inheritance）。真实 ① 尽力（读 compiler.label_proposals），LLM 没提议则注入兜底标签 `fix_runtime_exception`。

**Tech Stack:** Python 3.11, asyncio, 真实 LLM(gpt-5.5 via LiteLLM) + 本地 Qwen embedding + module0.5 API。

**上游 spec:** `docs/superpowers/specs/2026-07-10-e2e-module0.5-real-chain-design.md`

---

## 前置说明（实现者必读）

- **运行**：`.venv/bin/python scripts/e2e_smoke.py "修复 Python SyntaxError 并通过测试验证" smoke_20.jsonl`（脚本已默认 `HF_HUB_OFFLINE=1` + fixture 参数化;真实 LLM 需 `.env` 里的 LITELLM_BASE/KEY + MODULE0_TEST_MODEL=gpt-5.5,已就位）。
- **这不是单元测试任务**：e2e_smoke.py 是真实 LLM 演示脚本,验证靠人工检视输出。TDD 不适用;本计划的"验证"是"跑脚本、看 0.5 段输出符合成功标准"。
- **零 src 改动**：只改 `scripts/e2e_smoke.py`。
- **插入位置**：在 `# ── Stats ──` 块（现 line 256）**之前**、`async with LLMGateway(config) as gw:` 块**之内**（缩进对齐 module0/1/2/3 各段,即 8 空格缩进的方法体内）。此处这些变量都在作用域:`gw`(gateway)、`emb`(EmbeddingModel)、`taxonomy`(v0 Taxonomy)、`compiler`(QueryCompiler,已 compile 过,持有 `.label_proposals`)、`pipeline`(TrajectoryPipeline,`._store` 是含20条签名的 MemoryIndex,`._judge` 是真实 Judge)、`cfg`(PipelineConfig,`.recall_top_n`)。
- **复用的 helper**：`_print_section(title)`(line 65)打印分节标题。
- **需新增 import**：文件顶部 import 区（其它 `from moduleX import ...` 附近,遵守 noqa: E402 风格,因为在 load_dotenv 之后)加 module0.5 + TaxonomyStore 的导入。

## 文件结构

- **改 `scripts/e2e_smoke.py`**:
  - 顶部加 import：`from module0_5 import LabelProposal, ingest_proposal, run_backfill, rerank_with_inheritance`;`from module0.taxonomy import TaxonomyStore`。
  - main() 内 Stats 段之前追加一个 `_e2e_module0_5(...)` 调用 + 该异步 helper 函数（或直接内联一段）。本计划用**内联段**（与 module0/1/2/3 各段同风格,都是内联在 main() 里）。

---

## Task 1: 追加 import + module0.5 真实链路段

**Files:**
- Modify: `scripts/e2e_smoke.py`

- [ ] **Step 1: 加顶部 import**

在 `scripts/e2e_smoke.py` 现有 import 区（`from module3.selection import SelectionConfig  # noqa: E402` 那一行之后）追加:

```python
from module0.taxonomy import TaxonomyStore  # noqa: E402
from module0_5 import (  # noqa: E402
    LabelProposal,
    ingest_proposal,
    rerank_with_inheritance,
    run_backfill,
)
```

- [ ] **Step 2: 在 Stats 段之前插入 0.5 真实链路段**

在 main() 里,找到:

```python
        # ── Stats ────────────────────────────────────────────────────────
        _print_section("Gateway 统计")
        print(f"  {gw.http_stats}")
```

在这三行**之前**插入下面整段（8 空格缩进,与它同级,都在 `async with LLMGateway(...) as gw:` 块内）:

```python
        # ── Module 0.5: 标签演化真实链路 ─────────────────────────────────
        _print_section("模块0.5: ①提议捕获 → ②入库 → ③继承 → ④回填 → ③升权")

        # ① 提议捕获（真实,尽力而为）——读主链路那次 compiler 的产出
        real_proposals = compiler.label_proposals
        print(f"① module0 本次产出 label_proposals: {len(real_proposals)} 条")
        for p in real_proposals:
            print(f"    提议: {p.label} (parent={p.parent}) — {p.description[:40]}")

        # 选定新标签: 真实提议优先, 否则注入兜底 fix_runtime_exception
        if real_proposals:
            proposal = real_proposals[0]
            print(f"→ 使用真实提议: {proposal.label}")
        else:
            desc = "修复运行时抛出的异常，如 TypeError/KeyError/ValueError"
            proposal = LabelProposal(
                label="fix_runtime_exception",
                description=desc,
                parent="error_recovery",
                description_embedding=emb.embed(desc),
                keywords=["TypeError", "KeyError", "ValueError", "Traceback", "RuntimeError"],
                source_sub_problem_id="e2e_injected",
            )
            print(f"→ module0 未提议, 注入兜底标签: {proposal.label}")

        # ② 去重+挂载入库（真实 embedding）
        store = TaxonomyStore(taxonomy)
        res = ingest_proposal(proposal, store, created_at="2026-07-10T00:00:00Z")
        print(f"\n② 入库判定: kind={res.kind}, parent={res.parent}, "
              f"maps_to={res.maps_to}")
        print(f"   入库后 taxonomy version: {store.snapshot().version}")

        if res.kind == "duplicate":
            print("   提议判为重复(映射到已有标签), 无新标签入库, 跳过 ③④。")
        else:
            # ③ 回填前: 继承降权兜底（真实召回）
            def _recall_for_label():
                return pipeline._store.recall(
                    structured_filters={},
                    keywords=proposal.keywords,
                    query_embeddings=[proposal.description_embedding],
                    top_n=cfg.recall_top_n,
                )

            hits_before = _recall_for_label()
            before = rerank_with_inheritance(
                hits_before, target_label=proposal.label, taxonomy=store.snapshot())
            print(f"\n③ 回填前 rerank (target={proposal.label}), top 5:")
            for h in before[:5]:
                print(f"    {h.signature.trajectory_id}/slice{h.signature.slice_index} "
                      f"score={h.rrf_score:.4f} labels={h.signature.capability_labels}")

            # ④ 异步回填（真实 judge）
            new_label = store.snapshot().get(proposal.label)
            bf = await run_backfill(new_label, pipeline._store, pipeline._judge)
            print(f"\n④ 回填结果: screened={bf.candidates_screened}, "
                  f"judged_true={bf.judged_true}, written={bf.slices_written}, "
                  f"errors={bf.errors}")

            # ③ 回填后: 精确升权（对比）
            hits_after = _recall_for_label()
            after = rerank_with_inheritance(
                hits_after, target_label=proposal.label, taxonomy=store.snapshot())
            print(f"\n③ 回填后 rerank (target={proposal.label}), top 5:")
            for h in after[:5]:
                mark = " ← 精确命中(×1.0)" if proposal.label in (h.signature.capability_labels or []) else ""
                print(f"    {h.signature.trajectory_id}/slice{h.signature.slice_index} "
                      f"score={h.rrf_score:.4f}{mark}")

```

注意:上面段落末尾留一个空行,与其后的 `# ── Stats ──` 段隔开。

- [ ] **Step 3: 静态检查(ruff + 语法)**

Run: `.venv/bin/python -m ruff check scripts/e2e_smoke.py`
Expected: All checks passed!

Run: `.venv/bin/python -c "import ast; ast.parse(open('scripts/e2e_smoke.py').read()); print('syntax ok')"`
Expected: `syntax ok`

- [ ] **Step 4: 真实跑一次,人工检视 0.5 段输出**

Run: `.venv/bin/python scripts/e2e_smoke.py "修复 Python SyntaxError 并通过测试验证" smoke_20.jsonl`

Expected（0.5 段应出现,符合 spec §6 成功标准）:
- `模块0.5:` 分节标题打印。
- ① 打印 module0 本次产出的 label_proposals 条数(真实结果,可能 0)。
- 打印用的是"真实提议"还是"注入兜底标签"。
- ② `入库判定: kind=new_leaf, parent=error_recovery`(兜底标签 fix_runtime_exception 挂 error_recovery;若走真实提议则 kind 视 LLM 提议而定)。
- ④ `judged_true >= 1`(真实 judge 在 smoke_008/009/013/015/018/020 这类运行时错误轨迹上命中——前提是用兜底标签或真实提议指向运行时异常修复)。
- ③ 回填后 top 5 里至少一条带 `← 精确命中(×1.0)` 标记,且它在回填前不是精确命中(展示 ×0.3→×1.0 升权)。

**若 judged_true == 0**（真实 judge 一条都没判 true）:这是真实 judge 行为,记录到验证报告,不算脚本 bug。但先检查召回是否为空(hits_before 应非空,因为 keywords 含 TypeError 等能命中运行时错误轨迹的 bm25_tokens)。若召回非空但 judge 全 false,如实报告"真实 judge 未在现有语料命中新能力",这本身是有效观测。

- [ ] **Step 5: 提交**

```bash
git add scripts/e2e_smoke.py
git commit -m "feat(e2e): add module0.5 real-chain validation segment

Append ①proposal capture → ②ingest → ③inheritance → ④real-judge backfill
→ ③exact upgrade to e2e_smoke.py. Real ① best-effort (reads
compiler.label_proposals); injects fix_runtime_exception fallback when the
LLM proposes nothing. Reuses in-scope emb/taxonomy/pipeline/judge. Zero src
changes."
```

---

## Self-Review（已执行）

**1. Spec coverage** — 逐条对照 spec:
- §4 数据流 ①→选标签→②→③前→④→③后 → Task 1 Step 2 段落逐段对应 ✓
- §2 决策1(追加到 e2e_smoke) → 内联段 ✓;决策2(真实尽力+兜底) → `if real_proposals: ... else: 注入` ✓;决策3(fix_runtime_exception) → 兜底分支 ✓
- §3 兜底标签(label/description/parent/keywords/embedding=emb.embed) → 与 spec §3 代码块字段完全一致 ✓
- §5 关键点:judge 复用 `pipeline._judge` ✓;embedding 复用 `emb` ✓;taxonomy 复用 `TaxonomyStore(taxonomy)` ✓;created_at 注入固定串 ✓;① 如实报告 ✓;零 src 改动 ✓
- §6 成功标准 → Step 4 Expected 逐条覆盖(new_leaf / judged_true≥1 / 升权对比 / ①如实打印) ✓
- §7 验证方式 → Step 3(ruff)+ Step 4(真实跑) ✓

**2. Placeholder scan** — 无 TBD/TODO;插入段是完整可运行代码;判空/duplicate 分支都处理了。

**3. Type/API consistency**(已对真实代码验证,见 spec 自查):
- `ingest_proposal(proposal, store, *, created_at=)` ✓
- `run_backfill(new_label, store, judge)` ✓ — new_label 来自 `store.snapshot().get(...)`(TaxonomyLabel),不是 LabelProposal,类型对
- `rerank_with_inheritance(hits, *, target_label=, taxonomy=)` ✓
- `LabelProposal(label, description, parent, description_embedding, keywords, source_sub_problem_id)` 6 字段全 ✓
- `recall(structured_filters=, keywords=, query_embeddings=, top_n=)` 参数名 ✓
- `emb.embed(str)->list[float]` ✓;`pipeline._store`/`pipeline._judge`/`cfg.recall_top_n` 在作用域 ✓
- `res.kind`(Literal duplicate/new_leaf/new_root)/`res.parent`/`res.maps_to` ✓
- `BackfillResult.candidates_screened/judged_true/slices_written/errors` ✓
- `RecallHit.signature.trajectory_id/slice_index/capability_labels` + `.rrf_score` ✓

**一处实现者须注意**:`run_backfill` 的第一个参数必须是入库后的 `TaxonomyLabel`(从 `store.snapshot().get(proposal.label)` 取),不是原始 `LabelProposal`——两者字段不同。计划代码已正确用 `new_label = store.snapshot().get(...)`。

---

## 落地验证门

- ruff clean + 语法 ok(Step 3)。
- 真实跑出 0.5 段:② new_leaf、④ judged_true≥1、③ 升权对比可见(Step 4)。
- 若 judged_true==0 但召回非空:如实记录为真实 judge 观测,非脚本缺陷。
- 全程零 src 改动(仅 scripts/e2e_smoke.py)。
