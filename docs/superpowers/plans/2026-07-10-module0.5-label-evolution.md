# 模块 0.5 标签系统自演进 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现模块 0.5 标签系统自演进——① 新标签提议捕获、② 去重+挂载入库、③ 继承降权查询、④ 异步回填,让新标签从产生到全语料覆盖形成闭环。

**Architecture:** 新增 `src/module0_5/` 包(models/evolution/inheritance/backfill)+ `src/module0/taxonomy.py` 的 `TaxonomyStore` 可变层;模块 0 compiler 就地采集 `label_proposals` 经 side-channel 暴露。判定/重排/回填均为可独立单测的纯函数或注入式执行体,V1 用 `MemoryIndex` + `FakeEmbedder` 全程离线单测。

**Tech Stack:** Python 3.11, pytest, pytest-asyncio, numpy(cosine)。零真实 LLM/模型依赖(测试注入 FakeEmbedder + 假 gateway/judge)。

**上游 spec:** `docs/superpowers/specs/2026-07-10-module-0.5-label-evolution-design.md`(已按代码现状复核修订)

---

## 前置说明(实现者必读)

- **运行测试**:`/Users/deng/开发/UXFlow/.venv/bin/python -m pytest ...`(系统 `python` 是 3.9,会 import 失败)。
- 工作目录:`/Users/deng/开发/UXFlow`。本计划应在新分支实现(执行方按 subagent-driven 流程建分支)。
- **src/ 布局**:import 为顶层名(`module0`、`module1`、`module0_5`、`uxflow_embed`),不是 `src.xxx`。
- **打包**:实现完成后需把 `src/module0_5` 加入 `pyproject.toml` 的 `[tool.hatch.build.targets.wheel] packages`(Task 12)。
- **关键现有类型(已核对就位)**:
  - `TaxonomyLabel`(`src/module0/taxonomy.py:22`,`@dataclass(frozen=True)`):字段 `label, parent, new_root, description, keywords, description_embedding, taxonomy_extension, created_at`。
  - `Taxonomy`(taxonomy.py:35,只读):`version, updated_at, labels, get(name), leaf_labels(), to_prompt_text(), is_empty`,内部 `_by_name`。
  - `SliceStore`(`src/module1/store.py:24`,Protocol):`add_batch/recall/get_slice/set_slice_source/update_labels`。`RecallHit`(store.py:16):`signature, rrf_score`。
  - `MemoryIndex`(`src/module1/index.py`)实现 SliceStore,另有 `add(sig)`、`size`。`update_labels` 已是合并语义(`dict.fromkeys`,幂等)。
  - `TrajectorySignature`(`src/module1/models.py:47`):`trajectory_id, slice_index, bm25_tokens, embedding, capability_labels: list[str]|None`。
  - `Judge`(`src/module1/judge.py:35`):`async def judge_batch(self, *, slices, target_capability, trajectory_signal) -> list[JudgeResult]`。`JudgeResult`:`match, confidence, spans, reasoning`。
  - `FakeEmbedder`(`src/uxflow_embed/fake.py`):`FakeEmbedder(dimension=1024)`,`embed(text)->list[float]`(确定性、L2 归一),`embed_batch`。
  - compiler 现状:`self._embedding_model`(compiler.py:68);embedding 循环在 `compiler.py:154-157`;c2_results 路由循环 `compiler.py:119` 起;`_clarify_and_reeval` 在 `compiler.py:190`,返回 `list[SubProblem]`,其 c2p_results 循环在 `compiler.py:218` 起;`_build_sub_problem` 在 `compiler.py:230`。
- **⚠ fixture 陷阱**:`fixtures/taxonomy_v0.json` 的 `description_embedding` 是**空列表**。②(去重/挂载)测试需要已知 embedding 算 cosine,故测试必须**直接构造带已知 embedding 的 `TaxonomyLabel`**,不能依赖 v0 fixture 的 embedding。
- **时间戳约定**:纯逻辑内不产生时间(不用 `datetime.now()`);`created_at` 由调用方/编排层注入,判定函数与 store 不自造时间。

## 文件结构

**新增**
- `src/module0_5/__init__.py` — 导出 `LabelProposal, BackfillResult, resolve_proposal, ProposalResolution, rerank_with_inheritance, run_backfill`。
- `src/module0_5/models.py` — `LabelProposal`, `BackfillResult` 数据类。
- `src/module0_5/evolution.py` — `resolve_proposal` + `ProposalResolution`(② 判定,纯函数)。
- `src/module0_5/inheritance.py` — `rerank_with_inheritance`(③ 重排,纯函数)。
- `src/module0_5/backfill.py` — `run_backfill`(④ async 执行体)。
- `tests/module0_5/__init__.py` + `conftest.py` + 各测试文件。

**改动**
- `src/module0/taxonomy.py` — 新增 `TaxonomyStore` 可变层(`Taxonomy` 只读不动)。
- `src/module0/prompts.py` — Call 2/2' 输出 schema 加 `label_proposals` 槽位。
- `src/module0/parsing.py` — 显式保留 `label_proposals` + 测试锚定。
- `src/module0/compiler.py` — 就地采集 `label_proposals`,side-channel 暴露。
- `pyproject.toml` — packages 加 `src/module0_5`。
- `scripts/e2e_smoke.py` — 串入 0.5 演示段(可选,Task 13)。

## 任务分组(13 个任务,按依赖顺序)

- **A. 数据模型**:Task 1
- **B. ② 去重挂载 + TaxonomyStore**:Task 2-4
- **C. ③ 继承降权**:Task 5-6
- **D. ④ 异步回填**:Task 7-8
- **E. ① 提议捕获(模块 0 开口子)**:Task 9-11
- **F. 收尾**:Task 12(打包)、Task 13(e2e smoke,可选)

---

## Task 1: 数据模型 LabelProposal + BackfillResult

**Files:**
- Create: `src/module0_5/__init__.py`, `src/module0_5/models.py`
- Test: `tests/module0_5/__init__.py`, `tests/module0_5/test_models.py`

- [ ] **Step 1: 写失败测试** — 建 `tests/module0_5/__init__.py`(空),然后 `tests/module0_5/test_models.py`:

```python
from module0_5 import LabelProposal, BackfillResult


def test_label_proposal_fields():
    p = LabelProposal(
        label="handle_async_race",
        description="正确处理异步竞态",
        parent="error_recovery",
        description_embedding=[0.1, 0.2],
        keywords=["async", "race"],
        source_sub_problem_id="p1",
    )
    assert p.label == "handle_async_race"
    assert p.parent == "error_recovery"
    assert p.description_embedding == [0.1, 0.2]
    assert p.source_sub_problem_id == "p1"


def test_label_proposal_parent_optional():
    p = LabelProposal(
        label="x", description="d", parent=None,
        description_embedding=[], keywords=[], source_sub_problem_id="p2",
    )
    assert p.parent is None


def test_backfill_result_fields():
    r = BackfillResult(
        label="handle_async_race", candidates_screened=200,
        judged_true=12, slices_written=12, errors=[],
    )
    assert r.label == "handle_async_race"
    assert r.candidates_screened == 200
    assert r.judged_true == 12
    assert r.slices_written == 12
    assert r.errors == []
```

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/python -m pytest tests/module0_5/test_models.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'module0_5'`

- [ ] **Step 3: 写实现** — 建 `src/module0_5/models.py`:

```python
"""Module 0.5 data models — label proposals + backfill results."""
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 dengxuanliang

from __future__ import annotations

from dataclasses import dataclass, field

__all__ = ["LabelProposal", "BackfillResult"]


@dataclass
class LabelProposal:
    """模块0 side-channel 产出，0.5 ② 的输入。仅来自 passed 子问题。"""
    label: str
    description: str
    parent: str | None
    description_embedding: list[float]
    keywords: list[str]
    source_sub_problem_id: str


@dataclass
class BackfillResult:
    """④ 回填执行体的显式返回（不抛穿，失败可读）。"""
    label: str
    candidates_screened: int
    judged_true: int
    slices_written: int
    errors: list[str] = field(default_factory=list)
```

建 `src/module0_5/__init__.py`:

```python
"""Module 0.5 — label system self-evolution."""
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 dengxuanliang

from module0_5.models import LabelProposal, BackfillResult

__all__ = ["LabelProposal", "BackfillResult"]
```

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/module0_5/test_models.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 提交**

```bash
git add src/module0_5/__init__.py src/module0_5/models.py tests/module0_5/__init__.py tests/module0_5/test_models.py
git commit -m "feat(module0.5): add LabelProposal + BackfillResult data models"
```

---

## Task 2: TaxonomyStore 可变演化层

**Files:**
- Modify: `src/module0/taxonomy.py`（追加 `TaxonomyStore`，`Taxonomy` 不动）
- Test: `tests/module0_5/test_taxonomy_store.py`

- [ ] **Step 1: 写失败测试** — 建 `tests/module0_5/test_taxonomy_store.py`:

```python
from module0.taxonomy import Taxonomy, TaxonomyLabel, TaxonomyStore


def _lbl(name, parent=None, emb=None):
    return TaxonomyLabel(
        label=name, parent=parent, new_root=(parent is None),
        description=f"desc {name}", keywords=[name],
        description_embedding=emb or [0.0], taxonomy_extension=False,
        created_at="2026-01-01T00:00:00Z",
    )


def _base():
    return Taxonomy(version="0.1.0", updated_at="2026-01-01T00:00:00Z",
                    labels=[_lbl("error_recovery"), _lbl("effective_error_fix", "error_recovery")])


def test_snapshot_returns_readonly_taxonomy():
    store = TaxonomyStore(_base())
    snap = store.snapshot()
    assert isinstance(snap, Taxonomy)
    assert snap.get("error_recovery") is not None


def test_existing_labels_lists_current():
    store = TaxonomyStore(_base())
    names = {l.label for l in store.existing_labels()}
    assert names == {"error_recovery", "effective_error_fix"}


def test_add_label_appends_and_bumps_patch_version():
    store = TaxonomyStore(_base())
    new = _lbl("handle_async_race", "error_recovery")
    store.add_label(new)
    assert store.snapshot().get("handle_async_race") is not None
    assert store.snapshot().version == "0.1.1"  # patch +1


def test_add_label_does_not_touch_existing():
    store = TaxonomyStore(_base())
    store.add_label(_lbl("handle_async_race", "error_recovery"))
    assert store.snapshot().get("error_recovery").label == "error_recovery"


def test_save_writes_contract_json(tmp_path):
    import json
    store = TaxonomyStore(_base())
    store.add_label(_lbl("handle_async_race", "error_recovery"))
    out = tmp_path / "tax.json"
    store.save(out)
    data = json.loads(out.read_text())
    assert data["version"] == "0.1.1"
    labels = {l["label"] for l in data["labels"]}
    assert "handle_async_race" in labels
```

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/python -m pytest tests/module0_5/test_taxonomy_store.py -v`
Expected: FAIL — `ImportError: cannot import name 'TaxonomyStore'`

- [ ] **Step 3: 写实现** — 在 `src/module0/taxonomy.py` 末尾追加(并把 `TaxonomyStore` 加入 `__all__`):

先改文件顶部 `__all__`:
```python
__all__ = ["Taxonomy", "TaxonomyLabel", "TaxonomyStore"]
```

在文件末尾追加:
```python
class TaxonomyStore:
    """可变演化层。Taxonomy 保持只读；本类为唯一写入点。"""

    def __init__(self, taxonomy: Taxonomy):
        self._version = taxonomy.version
        self._updated_at = taxonomy.updated_at
        self._labels: list[TaxonomyLabel] = list(taxonomy.labels)

    def snapshot(self) -> Taxonomy:
        """产出只读快照供 prompt 注入 / 查询。"""
        return Taxonomy(
            version=self._version,
            updated_at=self._updated_at,
            labels=list(self._labels),
        )

    def existing_labels(self) -> list[TaxonomyLabel]:
        """当前全部标签（供 ② 算余弦）。"""
        return list(self._labels)

    def add_label(self, label: TaxonomyLabel) -> None:
        """追加新标签 + version patch +1。不影响已有条目。"""
        self._labels.append(label)
        self._version = _bump_patch(self._version)

    def save(self, path) -> None:
        """写回契约 §5.2 结构的 JSON。"""
        data = {
            "version": self._version,
            "updated_at": self._updated_at,
            "labels": [
                {
                    "label": l.label,
                    "parent": l.parent,
                    "new_root": l.new_root,
                    "description": l.description,
                    "keywords": l.keywords,
                    "description_embedding": l.description_embedding,
                    "taxonomy_extension": l.taxonomy_extension,
                    "created_at": l.created_at,
                }
                for l in self._labels
            ],
        }
        with open(path, "w") as f:
            json.dump(data, f, ensure_ascii=False, indent=2)


def _bump_patch(version: str) -> str:
    """Increment the patch component of a semver-ish 'a.b.c' string."""
    parts = version.split(".")
    if len(parts) != 3 or not parts[2].isdigit():
        return version  # non-standard version left as-is
    parts[2] = str(int(parts[2]) + 1)
    return ".".join(parts)
```

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/module0_5/test_taxonomy_store.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: 回归——现有 taxonomy 测试不受影响**

Run: `.venv/bin/python -m pytest tests/module0/test_taxonomy.py -q`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add src/module0/taxonomy.py tests/module0_5/test_taxonomy_store.py
git commit -m "feat(module0.5): add TaxonomyStore mutable evolution layer"
```

---

## Task 3: ② 去重+挂载判定 resolve_proposal

**Files:**
- Create: `src/module0_5/evolution.py`
- Modify: `src/module0_5/__init__.py`
- Test: `tests/module0_5/test_evolution.py`

- [ ] **Step 1: 写失败测试** — 建 `tests/module0_5/test_evolution.py`。构造已知 embedding(不依赖 v0 fixture):

```python
from module0.taxonomy import TaxonomyLabel
from module0_5 import LabelProposal
from module0_5.evolution import resolve_proposal, ProposalResolution


def _existing(name, parent, emb):
    return TaxonomyLabel(
        label=name, parent=parent, new_root=(parent is None),
        description=f"desc {name}", keywords=[name],
        description_embedding=emb, taxonomy_extension=False,
        created_at="2026-01-01T00:00:00Z",
    )


def _proposal(emb, parent="error_recovery"):
    return LabelProposal(
        label="new_leaf", description="新能力", parent=parent,
        description_embedding=emb, keywords=["kw"], source_sub_problem_id="p1",
    )


# top-level "error_recovery" at [1,0,0]; leaf under it at [0.9,0.1,0]
def _labels():
    return [
        _existing("error_recovery", None, [1.0, 0.0, 0.0]),
        _existing("effective_error_fix", "error_recovery", [0.9, 0.1, 0.0]),
    ]


def test_duplicate_when_cosine_above_dedup_threshold():
    # proposal nearly identical to existing leaf → duplicate, maps to it
    prop = _proposal([0.9, 0.1, 0.0])
    res = resolve_proposal(prop, _labels())
    assert res.kind == "duplicate"
    assert res.maps_to == "effective_error_fix"


def test_new_leaf_when_close_to_a_root():
    # not a dup, but cosine to root error_recovery >= 0.6 → mount under it
    prop = _proposal([0.8, 0.6, 0.0])  # cosine to [1,0,0] = 0.8/|v|
    res = resolve_proposal(prop, _labels())
    assert res.kind == "new_leaf"
    assert res.parent == "error_recovery"
    assert res.new_root is False


def test_new_root_when_far_from_all_roots():
    prop = _proposal([0.0, 0.0, 1.0])  # orthogonal to error_recovery root
    res = resolve_proposal(prop, _labels())
    assert res.kind == "new_root"
    assert res.parent is None
    assert res.new_root is True


def test_thresholds_are_configurable():
    prop = _proposal([0.9, 0.1, 0.0])
    # raise dedup threshold above the match so it is NOT a duplicate anymore
    res = resolve_proposal(prop, _labels(), dedup_threshold=0.999)
    assert res.kind != "duplicate"
```

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/python -m pytest tests/module0_5/test_evolution.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'module0_5.evolution'`

- [ ] **Step 3: 写实现** — 建 `src/module0_5/evolution.py`:

```python
"""Module 0.5 ② — dedup + mount decision for label proposals (pure, zero I/O)."""
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 dengxuanliang

from __future__ import annotations

from dataclasses import dataclass

import numpy as np

from module0.taxonomy import TaxonomyLabel
from module0_5.models import LabelProposal

__all__ = ["resolve_proposal", "ProposalResolution"]


@dataclass
class ProposalResolution:
    """② 判定结果。kind ∈ {duplicate, new_leaf, new_root}。"""
    kind: str
    maps_to: str | None = None      # duplicate: 映射到的已有标签名
    parent: str | None = None       # new_leaf: 挂载父节点；new_root: None
    new_root: bool = False


def _cosine(a: list[float], b: list[float]) -> float:
    va, vb = np.asarray(a, dtype=np.float64), np.asarray(b, dtype=np.float64)
    na, nb = np.linalg.norm(va), np.linalg.norm(vb)
    if na == 0 or nb == 0:
        return 0.0
    return float(va @ vb / (na * nb))


def resolve_proposal(
    proposal: LabelProposal,
    existing: list[TaxonomyLabel],
    *,
    dedup_threshold: float = 0.85,   # 契约 §5.3
    mount_threshold: float = 0.60,   # 契约 §5.3
) -> ProposalResolution:
    """判定：重复→映射已有 / 新叶子→挂父 / 新顶层→new_root。零写入。

    去重：与任一已有标签 cosine > dedup_threshold → duplicate。
    挂载：与顶层节点（parent is None）cosine >= mount_threshold → new_leaf（挂最相似）。
    否则 → new_root。embedding 相似度为权威；proposal.parent 仅在 cosine 接近时作参考。
    """
    emb = proposal.description_embedding

    # 去重：与所有已有标签比
    best_dup, best_dup_sim = None, -1.0
    for lbl in existing:
        sim = _cosine(emb, lbl.description_embedding)
        if sim > best_dup_sim:
            best_dup, best_dup_sim = lbl, sim
    if best_dup is not None and best_dup_sim > dedup_threshold:
        return ProposalResolution(kind="duplicate", maps_to=best_dup.label)

    # 挂载：只与顶层节点比
    roots = [l for l in existing if l.parent is None]
    best_root, best_root_sim = None, -1.0
    for root in roots:
        sim = _cosine(emb, root.description_embedding)
        if sim > best_root_sim:
            best_root, best_root_sim = root, sim
    if best_root is not None and best_root_sim >= mount_threshold:
        return ProposalResolution(kind="new_leaf", parent=best_root.label, new_root=False)

    return ProposalResolution(kind="new_root", parent=None, new_root=True)
```

更新 `src/module0_5/__init__.py`:
```python
"""Module 0.5 — label system self-evolution."""
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 dengxuanliang

from module0_5.models import LabelProposal, BackfillResult
from module0_5.evolution import resolve_proposal, ProposalResolution

__all__ = ["LabelProposal", "BackfillResult", "resolve_proposal", "ProposalResolution"]
```

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/module0_5/test_evolution.py -v`
Expected: PASS (4 passed)

> 若 `test_new_leaf_when_close_to_a_root` 的 cosine 边界不落在预期区间,微调测试向量使 cosine(prop, [1,0,0]) 落在 [0.6, 0.85) 内(该测试意图:非重复但够近于 root)。实现不变。

- [ ] **Step 5: 提交**

```bash
git add src/module0_5/evolution.py src/module0_5/__init__.py tests/module0_5/test_evolution.py
git commit -m "feat(module0.5): add resolve_proposal dedup/mount decision"
```

---

## Task 4: ② 入库编排(judged proposal → TaxonomyStore)

**Files:**
- Modify: `src/module0_5/evolution.py`（加 `ingest_proposal` 编排函数）
- Modify: `src/module0_5/__init__.py`
- Test: `tests/module0_5/test_evolution.py`（追加）

- [ ] **Step 1: 写失败测试** — 追加到 `tests/module0_5/test_evolution.py`:

```python
from module0.taxonomy import TaxonomyStore, Taxonomy
from module0_5.evolution import ingest_proposal


def _store():
    tax = Taxonomy(version="0.1.0", updated_at="2026-01-01T00:00:00Z", labels=_labels())
    return TaxonomyStore(tax)


def test_ingest_new_leaf_adds_to_store():
    store = _store()
    prop = _proposal([0.8, 0.6, 0.0])  # new_leaf under error_recovery
    res = ingest_proposal(prop, store, created_at="2026-07-10T00:00:00Z")
    assert res.kind == "new_leaf"
    added = store.snapshot().get("new_leaf")
    assert added is not None
    assert added.parent == "error_recovery"
    assert added.taxonomy_extension is True
    assert added.created_at == "2026-07-10T00:00:00Z"


def test_ingest_duplicate_does_not_add():
    store = _store()
    before = len(store.existing_labels())
    prop = _proposal([0.9, 0.1, 0.0])  # duplicate
    res = ingest_proposal(prop, store, created_at="2026-07-10T00:00:00Z")
    assert res.kind == "duplicate"
    assert len(store.existing_labels()) == before  # not added


def test_ingest_new_root_sets_new_root_flag():
    store = _store()
    prop = _proposal([0.0, 0.0, 1.0])  # new_root
    res = ingest_proposal(prop, store, created_at="2026-07-10T00:00:00Z")
    added = store.snapshot().get("new_leaf")
    assert added.new_root is True
    assert added.parent is None
```

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/python -m pytest tests/module0_5/test_evolution.py::test_ingest_new_leaf_adds_to_store -v`
Expected: FAIL — `ImportError: cannot import name 'ingest_proposal'`

- [ ] **Step 3: 写实现** — 在 `src/module0_5/evolution.py` 加入(并更新 `__all__`):

```python
from module0.taxonomy import TaxonomyStore  # 加到顶部 import
```
```python
def ingest_proposal(
    proposal: LabelProposal,
    store: TaxonomyStore,
    *,
    created_at: str,
    dedup_threshold: float = 0.85,
    mount_threshold: float = 0.60,
) -> ProposalResolution:
    """判定 + 写入。duplicate 丢弃不入库；new_leaf/new_root 转 TaxonomyLabel 入库。"""
    res = resolve_proposal(
        proposal, store.existing_labels(),
        dedup_threshold=dedup_threshold, mount_threshold=mount_threshold,
    )
    if res.kind == "duplicate":
        return res  # 丢弃不回填（决策）
    store.add_label(TaxonomyLabel(
        label=proposal.label,
        parent=res.parent,
        new_root=res.new_root,
        description=proposal.description,
        keywords=proposal.keywords,
        description_embedding=proposal.description_embedding,
        taxonomy_extension=True,
        created_at=created_at,
    ))
    return res
```
把 `evolution.py` 的 `__all__` 改为 `["resolve_proposal", "ProposalResolution", "ingest_proposal"]`;`module0_5/__init__.py` 的 import 与 `__all__` 追加 `ingest_proposal`。

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/module0_5/test_evolution.py -v`
Expected: PASS (7 passed — 4 旧 + 3 新)

- [ ] **Step 5: 提交**

```bash
git add src/module0_5/evolution.py src/module0_5/__init__.py tests/module0_5/test_evolution.py
git commit -m "feat(module0.5): add ingest_proposal (resolve + write to TaxonomyStore)"
```

---

## Task 5: ③ 继承降权 rerank_with_inheritance(基础打分)

**Files:**
- Create: `src/module0_5/inheritance.py`
- Modify: `src/module0_5/__init__.py`
- Test: `tests/module0_5/test_inheritance.py`

- [ ] **Step 1: 写失败测试** — 建 `tests/module0_5/test_inheritance.py`。用轻量假对象构造 RecallHit(避免造完整 TrajectorySignature):

```python
from module0.taxonomy import Taxonomy, TaxonomyLabel
from module0_5.inheritance import rerank_with_inheritance


class _Sig:
    def __init__(self, tid, labels):
        self.trajectory_id = tid
        self.capability_labels = labels


class _Hit:
    def __init__(self, tid, labels, score):
        self.signature = _Sig(tid, labels)
        self.rrf_score = score


def _lbl(name, parent):
    return TaxonomyLabel(
        label=name, parent=parent, new_root=(parent is None),
        description=f"d{name}", keywords=[], description_embedding=[],
        taxonomy_extension=False, created_at="",
    )


# error_recovery (root) → effective_error_fix, handle_async_race (siblings)
def _tax():
    return Taxonomy(version="0.1.0", updated_at="", labels=[
        _lbl("error_recovery", None),
        _lbl("effective_error_fix", "error_recovery"),
        _lbl("handle_async_race", "error_recovery"),
    ])


def test_exact_label_gets_full_weight():
    hits = [_Hit("A", ["handle_async_race"], 1.0)]
    out = rerank_with_inheritance(hits, target_label="handle_async_race", taxonomy=_tax())
    assert out[0].rrf_score == 1.0  # ×1.0


def test_sibling_leaf_gets_inherited_weight():
    # slice labeled with a sibling leaf (same parent) → ×0.3
    hits = [_Hit("B", ["effective_error_fix"], 1.0)]
    out = rerank_with_inheritance(hits, target_label="handle_async_race", taxonomy=_tax())
    assert abs(out[0].rrf_score - 0.3) < 1e-9


def test_parent_labeled_gets_inherited_weight():
    hits = [_Hit("C", ["error_recovery"], 1.0)]
    out = rerank_with_inheritance(hits, target_label="handle_async_race", taxonomy=_tax())
    assert abs(out[0].rrf_score - 0.3) < 1e-9


def test_unrelated_label_no_boost_no_penalty():
    hits = [_Hit("D", ["some_other_label"], 1.0)]
    out = rerank_with_inheritance(hits, target_label="handle_async_race", taxonomy=_tax())
    assert out[0].rrf_score == 1.0  # ×1.0 (只靠召回分)


def test_none_capability_labels_treated_as_unrelated():
    hits = [_Hit("E", None, 1.0)]
    out = rerank_with_inheritance(hits, target_label="handle_async_race", taxonomy=_tax())
    assert out[0].rrf_score == 1.0


def test_results_sorted_desc_by_weighted_score():
    hits = [_Hit("B", ["effective_error_fix"], 1.0), _Hit("A", ["handle_async_race"], 1.0)]
    out = rerank_with_inheritance(hits, target_label="handle_async_race", taxonomy=_tax())
    assert out[0].signature.trajectory_id == "A"  # exact(1.0) before inherited(0.3)
```

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/python -m pytest tests/module0_5/test_inheritance.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'module0_5.inheritance'`

- [ ] **Step 3: 写实现** — 建 `src/module0_5/inheritance.py`:

```python
"""Module 0.5 ③ — inheritance-decayed rerank (query-time, zero LLM, pure)."""
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 dengxuanliang

from __future__ import annotations

from module0.taxonomy import Taxonomy

__all__ = ["rerank_with_inheritance"]


def _family(target_label: str, taxonomy: Taxonomy) -> set[str]:
    """target 的父 + 同父兄弟叶子（含 target 自身之外的）集合，用于继承判定。"""
    target = taxonomy.get(target_label)
    if target is None or target.parent is None:
        return set()
    parent = target.parent
    fam = {parent}
    for lbl in taxonomy.labels:
        if lbl.parent == parent:
            fam.add(lbl.label)
    fam.discard(target_label)
    return fam


def rerank_with_inheritance(
    hits: list,
    *,
    target_label: str,
    taxonomy: Taxonomy,
    exact_weight: float = 1.0,
    inherited_weight: float = 0.3,   # 契约 §5.3
) -> list:
    """对已召回候选按 capability_labels + taxonomy 树加权重排。零 LLM。

    切片含 target_label 本身 → ×exact_weight；
    含 target 的父/兄弟叶子 → ×inherited_weight；
    都不含 → ×1.0（不加成不惩罚，仅靠召回分）。
    权重作用在 hit.rrf_score 上，重排后按加权分降序返回。
    """
    family = _family(target_label, taxonomy)
    rescored = []
    for hit in hits:
        labels = hit.signature.capability_labels or []
        if target_label in labels:
            weight = exact_weight
        elif family & set(labels):
            weight = inherited_weight
        else:
            weight = 1.0
        hit.rrf_score = hit.rrf_score * weight
        rescored.append(hit)
    rescored.sort(key=lambda h: h.rrf_score, reverse=True)
    return rescored
```

更新 `src/module0_5/__init__.py` import + `__all__` 追加 `rerank_with_inheritance`。

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/module0_5/test_inheritance.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: 提交**

```bash
git add src/module0_5/inheritance.py src/module0_5/__init__.py tests/module0_5/test_inheritance.py
git commit -m "feat(module0.5): add rerank_with_inheritance (③ inheritance decay)"
```

---

## Task 6: ③ 继承降权——回填后升权验证(过渡态→精确态)

**Files:**
- Test: `tests/module0_5/test_inheritance.py`（追加）

> 纯测试任务:验证 spec §5.4「③ 与 ④ 是同一打分逻辑的两个时间点」——回填给切片补上 target_label 后,同一 rerank 从 ×0.3 升到 ×1.0。实现已在 Task 5 完成。

- [ ] **Step 1: 写测试** — 追加到 `tests/module0_5/test_inheritance.py`:

```python
def test_backfill_upgrades_inherited_to_exact():
    # Before backfill: slice labeled only with parent → ×0.3
    hit = _Hit("C", ["error_recovery"], 1.0)
    out1 = rerank_with_inheritance([hit], target_label="handle_async_race", taxonomy=_tax())
    assert abs(out1[0].rrf_score - 0.3) < 1e-9

    # After backfill: same slice now carries target_label → ×1.0
    hit2 = _Hit("C", ["error_recovery", "handle_async_race"], 1.0)
    out2 = rerank_with_inheritance([hit2], target_label="handle_async_race", taxonomy=_tax())
    assert out2[0].rrf_score == 1.0
```

- [ ] **Step 2: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/module0_5/test_inheritance.py::test_backfill_upgrades_inherited_to_exact -v`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add tests/module0_5/test_inheritance.py
git commit -m "test(module0.5): verify backfill upgrades inherited weight to exact"
```

---

## Task 7: ④ 异步回填执行体 run_backfill（4 步)

**Files:**
- Create: `src/module0_5/backfill.py`
- Modify: `src/module0_5/__init__.py`
- Test: `tests/module0_5/conftest.py`, `tests/module0_5/test_backfill.py`

- [ ] **Step 1: 写测试脚手架 conftest** — 建 `tests/module0_5/conftest.py`(假 judge + 真 MemoryIndex + 造签名的工具):

```python
import pytest

from module1.index import MemoryIndex
from module1.models import Slice, TrajectorySignature
from module1.judge import JudgeResult


class FakeJudge:
    """judge_batch returns scripted match booleans by slice order."""
    def __init__(self, matches):
        self._matches = matches

    async def judge_batch(self, *, slices, target_capability, trajectory_signal):
        out = []
        for i, _ in enumerate(slices):
            m = self._matches[i] if i < len(self._matches) else False
            out.append(JudgeResult(match=m, confidence=0.9 if m else 0.0, spans=[], reasoning=""))
        return out


def _make_sig(tid, idx, tokens, emb, labels=None):
    return TrajectorySignature(
        trajectory_id=tid, slice_index=idx,
        languages=["python"], tools_used=["Edit"], turn_count=3,
        has_verification_step=True, slice_type="edit",
        bm25_tokens=tokens, embedding=emb, capability_labels=labels,
    )


def _make_slice(tid, idx):
    return Slice(trajectory_id=tid, slice_index=idx, steps=[], slice_type="edit")


@pytest.fixture
def populated_index():
    """MemoryIndex with 3 slices; A/B match 'async race', C is unrelated."""
    idx = MemoryIndex()
    for sig, sl in [
        (_make_sig("A", 0, ["async", "race"], [1.0, 0.0]), _make_slice("A", 0)),
        (_make_sig("B", 0, ["async", "race"], [0.9, 0.1]), _make_slice("B", 0)),
        (_make_sig("C", 0, ["unrelated"], [0.0, 1.0]), _make_slice("C", 0)),
    ]:
        idx.add(sig)
        idx.set_slice_source(sig.trajectory_id, sig.slice_index, sl)
    return idx
```

> ⚠ 实现者:先确认 `TrajectorySignature` 和 `Slice` 的**真实必填字段**(读 `src/module1/models.py`),按实际字段调整 `_make_sig`/`_make_slice`。上面是按常见字段的近似,字段名以 models.py 为准。

- [ ] **Step 2: 写失败测试** — 建 `tests/module0_5/test_backfill.py`:

```python
import pytest

from module0.taxonomy import TaxonomyLabel
from module0_5.backfill import run_backfill


def _new_label():
    return TaxonomyLabel(
        label="handle_async_race", parent="error_recovery", new_root=False,
        description="正确处理异步竞态", keywords=["async", "race"],
        description_embedding=[1.0, 0.0], taxonomy_extension=True,
        created_at="2026-07-10T00:00:00Z",
    )


async def test_backfill_writes_labels_to_judged_true_slices(populated_index):
    from tests.module0_5.conftest import FakeJudge
    # recall top_k will surface A,B,C (keyword/vector); judge says A,B match, C not
    judge = FakeJudge(matches=[True, True, False])
    result = await run_backfill(_new_label(), populated_index, judge, top_k=10)
    assert result.label == "handle_async_race"
    assert result.slices_written >= 1
    # A and B now carry the new label; C does not
    a = populated_index._get_signature("A", 0)
    assert "handle_async_race" in (a.capability_labels or [])
    c = populated_index._get_signature("C", 0)
    assert "handle_async_race" not in (c.capability_labels or [])


async def test_backfill_is_idempotent(populated_index):
    from tests.module0_5.conftest import FakeJudge
    judge = FakeJudge(matches=[True, True, False])
    await run_backfill(_new_label(), populated_index, judge, top_k=10)
    a1 = list(populated_index._get_signature("A", 0).capability_labels)
    # second run must not duplicate the label
    await run_backfill(_new_label(), populated_index, judge, top_k=10)
    a2 = populated_index._get_signature("A", 0).capability_labels
    assert a2.count("handle_async_race") == 1
    assert a1 == a2


async def test_backfill_result_counts(populated_index):
    from tests.module0_5.conftest import FakeJudge
    judge = FakeJudge(matches=[True, True, False])
    result = await run_backfill(_new_label(), populated_index, judge, top_k=10)
    assert result.candidates_screened >= 2
    assert result.judged_true == 2
```

> 注:`_get_signature` 是 MemoryIndex 的私有方法(index.py:98),测试用它读回写结果是可接受的白盒断言。

- [ ] **Step 3: 运行确认失败**

Run: `.venv/bin/python -m pytest tests/module0_5/test_backfill.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'module0_5.backfill'`

- [ ] **Step 4: 写实现** — 建 `src/module0_5/backfill.py`:

```python
"""Module 0.5 ④ — async targeted backfill (queue-schedulable execution body)."""
# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 dengxuanliang

from __future__ import annotations

from module0.taxonomy import TaxonomyLabel
from module0_5.models import BackfillResult

__all__ = ["run_backfill"]


async def run_backfill(
    new_label: TaxonomyLabel,
    store,
    judge,
    *,
    top_k: int = 200,
    judge_batch_size: int = 30,
) -> BackfillResult:
    """4 步回填一个标签。幂等：已标该 label 的切片跳过。失败可返回不抛穿。

    Step 1 构造查询（零LLM）：label.keywords → BM25；description_embedding → 向量锚。
    Step 2 粗筛（零LLM）：store.recall(...) 取 top_k 候选，跳过已标该 label 的。
    Step 3 精判（LLM）：judge.judge_batch 判该切片是否演示此能力。
    Step 4 写回：judged_true 的 → store.update_labels（合并语义，幂等）。
    """
    errors: list[str] = []
    label = new_label.label

    # Step 1 + 2: recall candidates
    query_embeddings = [new_label.description_embedding] if new_label.description_embedding else []
    try:
        hits = store.recall(
            structured_filters={},
            keywords=new_label.keywords,
            query_embeddings=query_embeddings,
            top_n=top_k,
        )
    except Exception as e:  # recall failure is recoverable — report, don't crash
        return BackfillResult(label=label, candidates_screened=0,
                              judged_true=0, slices_written=0, errors=[f"recall: {e}"])

    # skip slices already carrying the label (idempotence)
    candidates = []
    slices = []
    for hit in hits:
        sig = hit.signature
        if label in (sig.capability_labels or []):
            continue
        sl = store.get_slice(sig.trajectory_id, sig.slice_index)
        if sl is not None:
            candidates.append(sig)
            slices.append(sl)

    if not slices:
        return BackfillResult(label=label, candidates_screened=0,
                              judged_true=0, slices_written=0, errors=errors)

    # Step 3: LLM judge (reuse module1 Judge)
    try:
        results = await judge.judge_batch(
            slices=slices,
            target_capability=[label],
            trajectory_signal=new_label.description,
        )
    except Exception as e:
        return BackfillResult(label=label, candidates_screened=len(slices),
                              judged_true=0, slices_written=0, errors=[f"judge: {e}"])

    # Step 4: write back judged-true
    judged_true = 0
    written = 0
    for sig, res in zip(candidates, results):
        if res.match:
            judged_true += 1
            try:
                store.update_labels(sig.trajectory_id, sig.slice_index, [label])
                written += 1
            except Exception as e:
                errors.append(f"writeback {sig.trajectory_id}:{sig.slice_index}: {e}")

    return BackfillResult(
        label=label, candidates_screened=len(slices),
        judged_true=judged_true, slices_written=written, errors=errors,
    )
```

更新 `src/module0_5/__init__.py` import + `__all__` 追加 `run_backfill`。

- [ ] **Step 5: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/module0_5/test_backfill.py -v`
Expected: PASS (3 passed)

> 若 recall 未召回预期候选(BM25/向量在 2 维假向量上的行为),调整 conftest 里 `populated_index` 的 tokens/embedding 使 A/B 能被 `keywords=["async","race"]` 或向量锚召回。实现不变。

- [ ] **Step 6: 提交**

```bash
git add src/module0_5/backfill.py src/module0_5/__init__.py tests/module0_5/conftest.py tests/module0_5/test_backfill.py
git commit -m "feat(module0.5): add run_backfill async execution body (④ 4-step)"
```

---

## Task 8: ④ 回填失败可返回(judge 异常不抛穿)

**Files:**
- Test: `tests/module0_5/test_backfill.py`（追加）

> 纯测试:验证 spec §6.3「失败可返回」——judge 抛异常时 run_backfill 返回带 errors 的 BackfillResult 而非崩溃。实现已在 Task 7 的 try/except 完成。

- [ ] **Step 1: 写测试** — 追加到 `tests/module0_5/test_backfill.py`:

```python
async def test_backfill_judge_failure_returns_errors_not_crash(populated_index):
    class ExplodingJudge:
        async def judge_batch(self, **kwargs):
            raise RuntimeError("llm down")

    result = await run_backfill(_new_label(), populated_index, ExplodingJudge(), top_k=10)
    assert result.slices_written == 0
    assert any("judge" in e for e in result.errors)
```

- [ ] **Step 2: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/module0_5/test_backfill.py::test_backfill_judge_failure_returns_errors_not_crash -v`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add tests/module0_5/test_backfill.py
git commit -m "test(module0.5): backfill judge failure returns errors, no crash"
```

---

## Task 9: ① 改动 1 —— prompts.py 输出 schema 开 label_proposals 槽位

**Files:**
- Modify: `src/module0/prompts.py`
- Test: `tests/module0/test_prompts.py`（追加）

- [ ] **Step 1: 写失败测试** — 追加到 `tests/module0/test_prompts.py`(先看该文件现有测试风格,断言 prompt 文本含槽位说明):

```python
def test_call2_prompt_mentions_label_proposals_slot():
    from module0.prompts import build_call2_messages
    from module0.taxonomy import Taxonomy
    tax = Taxonomy(version="0.1.0", updated_at="", labels=[])
    msgs = build_call2_messages([{"id": "p1", "raw_text": "x", "failure_summary": "y"}], tax)
    text = " ".join(m["content"] for m in msgs)
    assert "label_proposals" in text
```

> 实现者:先读 `tests/module0/test_prompts.py` 和 `build_call2_messages` 的真实签名(可能参数不同),按实际签名调整测试调用。核心断言是输出 schema 文本含 `label_proposals`。

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/python -m pytest tests/module0/test_prompts.py::test_call2_prompt_mentions_label_proposals_slot -v`
Expected: FAIL(当前 prompt 无 `label_proposals`)

- [ ] **Step 3: 写实现** — 在 `src/module0/prompts.py` 的 Call 2 输出 JSON schema(约 `prompts.py:78-90`)与冷启动分支(约 `prompts.py:145-157`)中,给每个 item 的 JSON 示例增加可选字段:

```jsonc
  "route": "pass" 或 "drop",
  "label_proposals": [
    {"label": "new_label", "description": "一句话中文", "parent": "父建议或null", "keywords": ["kw1"], "taxonomy_extension": true}
  ]
```

并在正文提议说明处追加一句:"提议新标签时,除在 `target_capability` 用该标签名外,**必须**在 `label_proposals` 数组补齐 `{label, description, parent, keywords, taxonomy_extension:true}`;无新提议则省略该字段或给空数组。"两个分支(有词表 / 冷启动)都改。

> 注意:prompts.py 用 `{{`/`}}` 转义 f-string/format 的花括号——实现者需按该文件现有转义风格写 JSON 示例,避免 format 报错。改后本地跑一次 `build_call2_messages` 确认不抛异常。

- [ ] **Step 4: 运行确认通过 + 回归**

Run: `.venv/bin/python -m pytest tests/module0/test_prompts.py -q`
Expected: PASS(新测试 + 现有 prompt 测试全绿)

- [ ] **Step 5: 提交**

```bash
git add src/module0/prompts.py tests/module0/test_prompts.py
git commit -m "feat(module0): add label_proposals slot to Call 2/2' output schema"
```

---

## Task 10: ① 改动 2 —— parsing.py 保留 label_proposals(测试锚定)

**Files:**
- Test: `tests/module0/test_parsing.py`（追加）
- Modify: `src/module0/parsing.py`（仅在需要时；大概率零代码）

- [ ] **Step 1: 写测试** — 追加到 `tests/module0/test_parsing.py`:

```python
def test_parse_call2_preserves_label_proposals():
    from module0.parsing import parse_call2_response
    raw = '''[{
        "id": "p1",
        "target_capability": ["new_cap"],
        "trajectory_signal": "sig",
        "hyde_positive": ["seg1 twentychars.........", "seg2 twentychars........."],
        "keywords": ["k"],
        "structured_filters": {},
        "confidence": 0.9,
        "route": "pass",
        "label_proposals": [{"label": "new_cap", "description": "d", "parent": null, "keywords": ["k"], "taxonomy_extension": true}]
    }]'''
    items = parse_call2_response(raw)
    assert items[0]["label_proposals"][0]["label"] == "new_cap"


def test_parse_call2_ok_without_label_proposals():
    from module0.parsing import parse_call2_response
    raw = '''[{
        "id": "p1", "target_capability": ["c"], "trajectory_signal": "s",
        "hyde_positive": ["seg1 twentychars.........", "seg2 twentychars........."],
        "keywords": ["k"], "structured_filters": {}, "confidence": 0.9, "route": "pass"
    }]'''
    items = parse_call2_response(raw)
    assert "label_proposals" not in items[0]  # optional, absent is fine
```

- [ ] **Step 2: 运行确认**

Run: `.venv/bin/python -m pytest tests/module0/test_parsing.py::test_parse_call2_preserves_label_proposals tests/module0/test_parsing.py::test_parse_call2_ok_without_label_proposals -v`
Expected: PASS(解析层本就 `return items` 保留额外键——大概率**直接通过**,无需改代码。这两个测试是锚定"额外键被保留 + 缺字段不报错"的契约)。

> 若 `test_parse_call2_preserves_label_proposals` 失败(说明有意外剥离),再改 `parse_call2_response` 保留该键。否则不改代码。

- [ ] **Step 3: 提交**

```bash
git add tests/module0/test_parsing.py
git commit -m "test(module0): anchor that parse_call2 preserves optional label_proposals"
```

---

## Task 11: ① 改动 3 —— compiler.py 就地采集 label_proposals + side-channel

**Files:**
- Modify: `src/module0/compiler.py`
- Test: `tests/module0/test_compiler.py`（追加）

- [ ] **Step 1: 写失败测试** — 追加到 `tests/module0/test_compiler.py`。注入 FakeEmbedder + 带 label_proposals 的 Call 2 响应:

```python
from uxflow_embed import FakeEmbedder


_C2_WITH_PROPOSAL = '''[{
    "id": "p1",
    "target_capability": ["new_cap"],
    "trajectory_signal": "observation 含 SyntaxError",
    "hyde_positive": ["正例片段一,超过二十字符的假设轨迹", "正例片段二,超过二十字符的假设轨迹"],
    "keywords": ["SyntaxError"],
    "structured_filters": {"languages": ["python"]},
    "confidence": 0.92,
    "route": "pass",
    "label_proposals": [{"label": "new_cap", "description": "新能力描述", "parent": "code_generation", "keywords": ["SyntaxError"], "taxonomy_extension": true}]
}]'''


async def test_compiler_collects_label_proposals(taxonomy):
    gw = FakeGateway([_C1_OK, _C2_WITH_PROPOSAL])
    compiler = QueryCompiler(gateway=gw, taxonomy=taxonomy, model="test-model",
                             embedding_model=FakeEmbedder(dimension=8))
    await compiler.compile("写入py文件有语法错误")
    assert len(compiler.label_proposals) == 1
    prop = compiler.label_proposals[0]
    assert prop.label == "new_cap"
    assert prop.parent == "code_generation"
    assert prop.source_sub_problem_id == "p1"
    assert len(prop.description_embedding) == 8  # FakeEmbedder filled it


async def test_compiler_no_proposals_when_none_offered(taxonomy):
    gw = FakeGateway([_C1_OK, _C2_OK])  # _C2_OK has no label_proposals
    compiler = QueryCompiler(gateway=gw, taxonomy=taxonomy, model="test-model",
                             embedding_model=FakeEmbedder(dimension=8))
    await compiler.compile("写入py文件有语法错误")
    assert compiler.label_proposals == []


async def test_compiler_drops_proposals_from_dropped_items(taxonomy):
    # a dropped (route=drop) item's proposals must NOT be collected
    c2_dropped = _C2_WITH_PROPOSAL.replace('"route": "pass"', '"route": "drop", "drop_reason": "not_applicable"').replace('"confidence": 0.92', '"confidence": 0.2')
    gw = FakeGateway([_C1_OK, c2_dropped])
    compiler = QueryCompiler(gateway=gw, taxonomy=taxonomy, model="test-model",
                             embedding_model=FakeEmbedder(dimension=8))
    await compiler.compile("写入py文件有语法错误")
    assert compiler.label_proposals == []
```

- [ ] **Step 2: 运行确认失败**

Run: `.venv/bin/python -m pytest tests/module0/test_compiler.py::test_compiler_collects_label_proposals -v`
Expected: FAIL — `AttributeError: 'QueryCompiler' object has no attribute 'label_proposals'`

- [ ] **Step 3: 写实现** — 改 `src/module0/compiler.py`:

(a) 顶部 import 加:
```python
from module0_5.models import LabelProposal
```

(b) `__init__` 末尾(现有 `self.hyde_embeddings = {}` 之后)加:
```python
        self.label_proposals: list[LabelProposal] = []
        self._collected_proposals: list[dict] = []
```

(c) `compile()` 开头重置区(现有 `self.hyde_embeddings = {}` 那几行之后)加:
```python
        self.label_proposals = []
        self._collected_proposals = []
```

(d) 主路径 Call 2 路由循环里,在 `passed.append(self._build_sub_problem(...))` 成功后(`compiler.py:119` 起的 `try` 块内、append 之后)加就地采集:
```python
                try:
                    passed.append(self._build_sub_problem(result, raw, origin="original", parent_id=None))
                    self._collect_proposals(result)   # 新增:就地采集该 passed item 的提议
                except ValueError:
                    self._record_dropped(result, raw, "other")
```

(e) `_clarify_and_reeval` 里,在 recover 成功后(遍历 c2p_results 构造 recovered 的循环内、`recovered.append(...)` 之后)加同样一行:
```python
                try:
                    recovered.append(self._build_sub_problem(result, raw, origin="clarified", parent_id=parent_map.get(sp_id)))
                    self._collect_proposals(result)   # 新增
                except ValueError:
                    pass
```

(f) 新增私有方法 `_collect_proposals`(放在 `_build_sub_problem` 附近):
```python
    def _collect_proposals(self, result: dict) -> None:
        """就地采集一个 passed/recovered item 的 label_proposals 原始 dict。"""
        for prop in result.get("label_proposals", []) or []:
            if prop.get("taxonomy_extension"):
                self._collected_proposals.append({
                    "label": prop.get("label", ""),
                    "description": prop.get("description", ""),
                    "parent": prop.get("parent"),
                    "keywords": prop.get("keywords", []),
                    "source_sub_problem_id": result.get("id", ""),
                })
```

(g) 在现有 embedding 循环(`compiler.py:154-157`)**之后**,加统一算 embedding 产出 LabelProposal:
```python
        # ── Label proposals: compute description embeddings (same model) ──
        for p in self._collected_proposals:
            emb = self._embedding_model.embed(p["description"]) if self._embedding_model is not None else []
            self.label_proposals.append(LabelProposal(
                label=p["label"], description=p["description"], parent=p["parent"],
                description_embedding=emb, keywords=p["keywords"],
                source_sub_problem_id=p["source_sub_problem_id"],
            ))
```

- [ ] **Step 4: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/module0/test_compiler.py::test_compiler_collects_label_proposals tests/module0/test_compiler.py::test_compiler_no_proposals_when_none_offered tests/module0/test_compiler.py::test_compiler_drops_proposals_from_dropped_items -v`
Expected: PASS (3 passed)

- [ ] **Step 5: 回归——现有 compiler 测试(含健壮性)不受影响**

Run: `.venv/bin/python -m pytest tests/module0/test_compiler.py -q`
Expected: PASS(全部)

- [ ] **Step 6: 提交**

```bash
git add src/module0/compiler.py tests/module0/test_compiler.py
git commit -m "feat(module0): collect label_proposals in-place, expose via side-channel"
```

---

## Task 12: 打包——module0_5 加入 wheel + 全量回归

**Files:**
- Modify: `pyproject.toml`
- Test: 全量纯逻辑回归

- [ ] **Step 1: 加包** — `pyproject.toml` 的 `[tool.hatch.build.targets.wheel]` packages 列表末尾加 `"src/module0_5"`:

```toml
packages = ["src/llm_gateway", "src/module0", "src/module1", "src/module2", "src/module3", "src/uxflow_embed", "src/module0_5"]
```

- [ ] **Step 2: 验证 import + TOML**

Run: `.venv/bin/python -c "import module0_5; from module0_5 import LabelProposal, BackfillResult, resolve_proposal, ingest_proposal, rerank_with_inheritance, run_backfill; print('ok')"`
Expected: `ok`

- [ ] **Step 3: 全量纯逻辑回归**

Run: `.venv/bin/python -m pytest -m "not requires_model" -q`
Expected: PASS(现有 229 + module0_5 新增,全绿)

- [ ] **Step 4: ruff**

Run: `.venv/bin/python -m ruff check src/module0_5 tests/module0_5 src/module0`
Expected: All checks passed(若有 lint 问题,修到干净)

- [ ] **Step 5: 提交**

```bash
git add pyproject.toml
git commit -m "build: add module0_5 to wheel packages"
```

---

## Task 13:（可选）端到端 smoke 演示 0.5 闭环

**Files:**
- Test: `tests/module0_5/test_evolution_e2e.py`

> spec §8 要求端到端演示:入库 → ③ 兜底非空 → ④ 回填后精确升权。用纯逻辑对象串起来(FakeEmbedder + MemoryIndex + FakeJudge),不碰真实 LLM。这是一个整合测试,验证四步接口彼此咬合。

- [ ] **Step 1: 写整合测试** — 建 `tests/module0_5/test_evolution_e2e.py`:

```python
import pytest

from module0.taxonomy import Taxonomy, TaxonomyLabel, TaxonomyStore
from module0_5 import LabelProposal, ingest_proposal, rerank_with_inheritance, run_backfill


def _tax_with_family():
    def lbl(name, parent, emb):
        return TaxonomyLabel(label=name, parent=parent, new_root=(parent is None),
                             description=f"d{name}", keywords=[name], description_embedding=emb,
                             taxonomy_extension=False, created_at="")
    return Taxonomy(version="0.1.0", updated_at="", labels=[
        lbl("error_recovery", None, [1.0, 0.0]),
        lbl("effective_error_fix", "error_recovery", [0.9, 0.1]),
    ])


async def test_0_5_closed_loop(populated_index):
    from tests.module0_5.conftest import FakeJudge

    # ② ingest a new leaf under error_recovery
    store = TaxonomyStore(_tax_with_family())
    prop = LabelProposal(label="handle_async_race", description="异步竞态",
                         parent="error_recovery", description_embedding=[0.8, 0.2],
                         keywords=["async", "race"], source_sub_problem_id="p1")
    res = ingest_proposal(prop, store, created_at="2026-07-10T00:00:00Z")
    assert res.kind == "new_leaf"

    # ④ backfill: judge marks A,B true → they get the label
    new_label = store.snapshot().get("handle_async_race")
    judge = FakeJudge(matches=[True, True, False])
    bf = await run_backfill(new_label, populated_index, judge, top_k=10)
    assert bf.slices_written >= 1

    # after backfill, a recall hit carrying the label reranks to exact ×1.0
    class _Hit:
        def __init__(self, tid, labels, score):
            class S: pass
            self.signature = S(); self.signature.trajectory_id = tid
            self.signature.capability_labels = labels
            self.rrf_score = score
    hits = [_Hit("A", ["handle_async_race"], 1.0)]
    out = rerank_with_inheritance(hits, target_label="handle_async_race", taxonomy=store.snapshot())
    assert out[0].rrf_score == 1.0
```

- [ ] **Step 2: 运行确认通过**

Run: `.venv/bin/python -m pytest tests/module0_5/test_evolution_e2e.py -v`
Expected: PASS

- [ ] **Step 3: 提交**

```bash
git add tests/module0_5/test_evolution_e2e.py
git commit -m "test(module0.5): end-to-end closed-loop (ingest → backfill → exact rerank)"
```

---

## Self-Review（已执行）

**1. Spec coverage** — 逐条对照 spec:
- §2 数据模型 LabelProposal/BackfillResult → Task 1 ✓
- §3 ① 提议捕获(改动1 prompts / 改动2 parsing / 改动3 compiler 就地采集) → Task 9/10/11 ✓（采集机制用修订后的「就地汇总到 self._collected_proposals」）
- §4 ② 去重挂载 + TaxonomyStore → Task 2(store)/3(resolve)/4(ingest) ✓
- §5 ③ 继承降权 → Task 5(基础)/6(升权验证) ✓
- §6 ④ 异步回填 4 步 + 幂等 + 失败可返回 → Task 7/8 ✓
- §7 触发编排 → Task 13 端到端串联 ✓
- §8 验证方式(MemoryIndex 上端到端 + 各步单测) → 全程 FakeEmbedder/MemoryIndex/FakeJudge ✓
- §9 文件清单 → Task 1-13 覆盖(含 pyproject Task 12)✓

**2. Placeholder scan** — 无 TBD/TODO;每步给完整代码 + 确切命令。两处标注「实现者需按 models.py 真实字段调整 conftest 造签名工具」「按 build_call2_messages 真实签名调整测试调用」——这不是占位,而是**诚实标注了我未逐字读取的两个接口**(TrajectorySignature/Slice 完整字段、build_call2_messages 签名),要求实现者以源码为准。已给出近似 + 校验方式。

**3. Type consistency** — `LabelProposal` 字段(label/description/parent/description_embedding/keywords/source_sub_problem_id)在 Task 1 定义,Task 3/4/11 使用一致;`ProposalResolution.kind` 取值 duplicate/new_leaf/new_root 在 Task 3 定义、Task 4 消费一致;`resolve_proposal`/`ingest_proposal`/`rerank_with_inheritance`/`run_backfill` 签名在各自 Task 定义,Task 13 调用一致;`TaxonomyStore` 方法(snapshot/existing_labels/add_label/save)Task 2 定义、Task 4/13 使用一致;`BackfillResult` 字段 Task 1 定义、Task 7/8 使用一致。

**已知实现者须核对的两个接口**(计划已标注):
1. `TrajectorySignature` / `Slice` 的完整必填字段(Task 7 conftest 的 `_make_sig`/`_make_slice`)——以 `src/module1/models.py` 为准。
2. `build_call2_messages` 的真实签名(Task 9 测试)——以 `src/module0/prompts.py` 为准。

---

## 落地顺序与验证门

- **Task 1-4 门**:数据模型 + ② 判定/入库/TaxonomyStore 全绿(纯逻辑,已知 embedding)。
- **Task 5-6 门**:③ 继承降权基础 + 升权验证全绿。
- **Task 7-8 门**:④ 回填在 MemoryIndex 上写回正确 + 幂等 + 失败可返回。
- **Task 9-11 门**:① 三处改动,compiler 就地采集提议 + 现有 compiler 测试(含健壮性)不回归。
- **Task 12 门**:module0_5 入包,全量 `pytest -m "not requires_model"` 全绿、ruff 干净。
- **Task 13 门**:端到端闭环整合测试通过。
- 全部完成后:模块 0.5 四步闭环就位,均可在无真实 LLM/模型下单测;真实持久化后端与作业队列为后续单独任务(spec §10)。
